[project]
name = "boardgames-trainer"
version = "0.1.0"
description = "Board-game RL consumer for modeltrainer — the §C rigorous-evaluation driving case."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
